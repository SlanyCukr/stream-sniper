'use client'

// Placeholder shell — replaced by the community-map implementation.
const Community = () => (
    <div>
        <div className="page-head">
            <div>
                <p className="page-sub">audience intersection across tracked creators</p>
                <h1 className="page-title">Community</h1>
            </div>
        </div>
        <div className="empty-state">
            <span
                className="empty-scope"
                aria-hidden="true"
            ></span>
            <p className="empty-title">Under construction</p>
            <p className="empty-hint">The community overlap map is being assembled.</p>
        </div>
    </div>
)

export default Community
