'use client'

// Placeholder shell — replaced by the highlight-queue implementation.
const Moments = () => (
    <div>
        <div className="page-head">
            <div>
                <p className="page-sub">chat spikes worth clipping</p>
                <h1 className="page-title">Highlights</h1>
            </div>
        </div>
        <div className="empty-state">
            <span
                className="empty-scope"
                aria-hidden="true"
            ></span>
            <p className="empty-title">Under construction</p>
            <p className="empty-hint">The highlight queue is being assembled.</p>
        </div>
    </div>
)

export default Moments
