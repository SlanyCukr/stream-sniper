- 2026-07-20 frontend: two parallel metric-tile systems render on the same stream page — `components/stream/report/streamMetricTiles.ts::buildStreamMetricTiles` (StreamMetrics) and `StreamReportPresentation.tsx::METRICS`/ReportMetricGrid (StreamReportCard) both build {label, value, phosphor, hint} arrays for the same StreamMetricTile with verbatim-duplicated labels (Total messages, Messages/min, Unique chatters, Peak minute, New/Returning). Consolidating needs a product/design call (fold into one panel or make one a strict supplement), not a mechanical dedup.
- 2026-07-20 frontend: `components/admin/tracking/jobs/ProcessingJobsTable.tsx` and `.../streamers/TrackedStreamerTable.tsx` share an identical Card/loading/empty/Table/footer scaffold — extractable as a `PaginatedTableCard` if a third admin table ever appears.
- 2026-07-20 frontend: `ChatterPanelShell` and `StreamReplayCard` deliberately keep hand-rolled state branching (specialized shells: awaiting-target pre-state/aria-live region, and composed non-query state with always-visible filters respectively) — they gate errors with `error && !isLoading`, so stale data is blanked on background refetch failure, diverging from QueryState's stale-data-wins policy. Converting means redesigning their prop contracts; every other panel was QueryState-converted 2026-07-20.
